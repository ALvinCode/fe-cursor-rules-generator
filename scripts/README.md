# Scripts 目录

本目录包含项目维护和开发辅助脚本。

## 📁 脚本列表

### 🔧 开发工具

#### `analyze-module-usage.js`
分析项目中模块的使用情况，识别未使用的模块。

**用法：**
```bash
node scripts/analyze-module-usage.js
```

**输出：**
- 未使用的模块列表
- 核心模块列表（被 index.ts 直接使用）
- 工具模块列表（仅被其他模块使用）
- 统计信息（总模块数、代码行数等）

#### `update-imports.js`
批量更新模块内部的 import 路径，用于目录重构后的路径修复。

**用法：**
```bash
node scripts/update-imports.js
```

**功能：**
- 自动检测模块的新位置
- 更新所有模块间的相对引用
- 支持跨子目录的引用更新

#### `fix-relative-paths.js`
修复模块相对路径引用问题。

**用法：**
```bash
node scripts/fix-relative-paths.js
```

**功能：**
- 修复 `../types.js` -> `../../types.js`
- 修复 `../utils/` -> `../../utils/`
- 处理特殊的跨目录引用

### 📦 发布工具

#### `prepare-release.js`
准备发布新版本。

**用法：**
```bash
npm run prepare-release
```

**功能：**
- 检查构建状态
- 验证测试通过
- 更新版本号
- 生成 CHANGELOG

## 🚀 常用命令

```bash
# 构建项目
npm run build

# 运行测试
npm test

# 类型检查
npm run type-check

# 准备发布
npm run prepare-release

# 分析模块使用情况
node scripts/analyze-module-usage.js
```

## 📝 注意事项

1. 所有脚本都应该在项目根目录运行
2. 某些脚本可能需要先执行 `npm run build`
3. 修改脚本后请更新此 README

## 🔄 最近更新

**2025-11-20**: 项目重构
- 添加了模块分析工具
- 添加了 import 路径更新工具
- 删除了过时的测试和规则抓取脚本
- 简化了脚本目录结构

